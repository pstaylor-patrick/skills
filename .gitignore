# Git worktrees created inside the repo
/.worktrees/
/worktrees/

# macOS / editor cruft
.DS_Store

# Node deps for the Pi hook extension toolchain
/node_modules/

# Per-device home pin written by install.rb; never committed (it holds an
# absolute user path). Only the installed copy under ~/.claude carries it.
.expected-home

# Local change-fabric team private key (cf_team_init.rb/cf_team_join.rb),
# base64 Ed25519 seed. Never committed; the public key lives in CHANGE.md.
.env.cf-team
