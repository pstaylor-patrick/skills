# Rails Reference

Deps tree: rails -> ruby (both auto-loaded)

## Common commands

- `bin/rails db:migrate` -- run pending migrations
- `bin/rails db:rollback` -- revert last migration
- `bundle exec rspec spec/` -- run all specs
- `bin/rails routes | grep <keyword>` -- find routes
