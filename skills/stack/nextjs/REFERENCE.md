# Next.js Reference

Deps tree: nextjs -> react -> typescript (all three auto-loaded)

## Common commands

- `pnpm dev` -- start dev server (run as Docker container per rule 20, not bare)
- `pnpm build` -- production build
- `pnpm lint` -- ESLint via next lint
