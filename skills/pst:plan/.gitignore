# Per-user config — copy plans.config.example.json → plans.config.json
plans.config.json

# Studio build + deps (studio/.gitignore also covers these)
studio/dist/
studio/node_modules/
studio/.astro/
studio/feedback/*.json

# Terraform state + locals
terraform/.terraform/
terraform/.terraform-build/
terraform/*.tfstate
terraform/*.tfstate.*
terraform/terraform.tfvars
terraform/.terraform.lock.hcl
