// Self-host the curated variable fonts so published artifacts don't depend on a
// font CDN. Imported once from PlanLayout; the active families are selected per
// theme via the --font-* CSS custom properties (see theme.ts).
import "@fontsource-variable/inter";
import "@fontsource-variable/sora";
import "@fontsource-variable/space-grotesk";
import "@fontsource-variable/fraunces";
import "@fontsource-variable/newsreader";
import "@fontsource-variable/jetbrains-mono";
